# GNSS Filter v8.0

Віддає навігатору **лише перевірену позицію**. Мережа (вишки, Wi-Fi) — якір,
GPS — кандидат, якого до якоря звіряють. Поки GPS не в довірі, mock-провайдер
стоїть завжди: спуфер до навігатора не дістає.

## Чому саме так

Польовий лог 13.09 (Note 20, 5 год, район активного РЕБ): GPS брехав наростаюче —
2 → 11 → 27 → 40 → 60 км, а під кінець перекинув у **Ліму, Перу**, 12 000 км.
При цьому **фізично** підмінений сигнал не відрізнявся від справжнього:
супутників у розв'язку 28 (проти 16 у справжнього), C/N0 однаковий, AGC просів
лише на 5 дБ. Упіймало його виключно **розходження з мережею**.

Той самий лог показав, що знімати mock без довіреного джерела — помилка:
навігатор одразу підхоплював спуфера.

## Довіра до GPS: три умови одночасно

1. **Супутників у розв'язку** ≥ 8 для набуття довіри, ≥ 5 для утримання.
2. **GPS у зоні мережі**. Зона двоярусна, бо на 4363 парах справжнього GPS
   проти мережі точна мережа (≤ 300 м) брехала не більш ніж у 4×, а груба —
   непередбачувано (заявлені 500 м, реальні 4 км):
   - точна: 3 × точність + 500 м;
   - груба (до 3 км): 4 × точність + 5 км.
   Ліму й 20 км ловлять обидві. Вихід за зону — вирок через 3 с; удвічі за
   зону або понад 5 км — негайно.
3. **Немає фальшивого руху**: акселерометр 5 с каже «стоїмо», а GPS показує
   понад 2 м/с. Єдиний фізичний доказ, що працює й без мережі.

Без мережі GPS не перевіряється. Під латчем підміни без мережі довіри не буде.
**Латч знімає тільки сходження**: 20 с поспіль GPS у зоні мережі з точністю ≤ 300 м.

## Стани

| Колір | Стан | Що отримує навігатор |
|---|---|---|
| зелений | GPS у довірі | справжній GPS, mock знято |
| жовтий | GPS слабкий / **проба** | справжній GPS під наглядом |
| помаранчевий | точна мережа | мережева позиція + швидкість + курс, 4 Гц |
| помаранчевий | груба мережа (до 3 км) | передбачення за швидкістю й курсом, фікс — лише слабка прив'язка |
| червоний | мережі немає — **утримання** | остання довірена позиція, радіус росте 5 м/с до 5 км |
| червоний | утримувати нічого | mock порожній: навігатор бачить «немає GPS», а не спуфера |

## Повернення довіри — проба

Під mock GPS не видно. Коли фізика чиста (супутники, AGC), mock знімається на
**пробу**. Без латча — 5 с чистих і довіра. З латчем — три яруси за тим, хто
може підтвердити GPS:

| Хто підтверджує | Проба не частіше | Чистих секунд | Тривалість проби |
|---|---|---|---|
| мережа ≤ 300 м | 20 с | 20 | 25 с |
| мережа до 3 км | 40 с | 40 | 50 с |
| нікого — лише фізика й акселерометр | 60 с | 60 | 75 с |

Інтервали короткі навмисно: проба обривається на **першій** ознаці, тому
невдала проба під активним спуфером коштує 1-2 с впливу, а не своєї повної
тривалості. Після трьох поспіль вироків «підміна» інтервал подвоюється до
стелі в 3 хв — спуфер нікуди не подівся, немає сенсу смикати mock щохвилини.
Будь-яка інша причина завершення пробу «пробачає»: лічильник спадає.

Будь-яка ознака під час проби — миттєво назад під mock. Третій ярус —
компроміс: без нього застосунок за містом після першої підміни не повернувся б.

Прогін логу з Лімою: секунд брехні у навігаторі понад 2 км — **1050 → ~300**
за п'ять годин (~1,8%), і повернення довіри через 2 хв після того, як GPS
повернувся до реальності (у 5.5 — не повернулась до зупинки).

## OBD-адаптер (ELM327, Bluetooth)

Закриває єдину дірку, яку не закрити нічим у телефоні: **незалежний доказ руху**.
Читається один параметр — швидкість з коліс, PID `01 0D`, чотири рази на секунду.

- Адаптер спарюється в системних налаштуваннях Bluetooth (PIN `1234` або `0000`).
  У застосунку: галочка «OBD-адаптер» і кнопка «Обрати адаптер» — або авто-вибір
  за назвою (OBD, ELM, V-LINK…).
- Швидкість з OBD **керує детектором руху** замість акселерометра.
- **Фальшивий рух** з OBD: колеса стоять, GPS «їде» понад 2 м/с — вирок за 5 с
  замість 10.
- **Розбіжність швидкостей**: GPS і OBD різняться понад 15 км/год або 30% —
  голос через 10 с, вирок через 20. Це ловить спуфера, що веде «не з нашою
  швидкістю», навіть коли ми справді їдемо і мережі немає.
- В екстраполяції довжина береться з коліс: похибка швидкості 1-2% замість 30%.

Обрив зв'язку — буденність: перепідключення з наростаючою паузою, і поки
зв'язку немає, фільтр працює як без OBD. Стан видно в рядку «OBD:».

## Гіроскоп — курс між фіксами мережі

Кутова швидкість інтегрується один раз (не двічі, як прискорення), тому
поворот на 90° лишається 90° і через хвилину. Дрейф ~1°/хв. Проєкція на вісь
тяжіння робить орієнтацію телефона в тримачі неважливою.

Курс сіється зі справжнього GPS (у довірі), підправляється мережевим вектором
(20% на кожному значущому зсуві), і задає **напрямок** екстраполяції, а OBD —
**довжину**. Разом це числення шляху, яке мережа не дає втекти.

Знак інтегрування виведений з документації Android (додатне обертання проти
годинникової), але емпірично ще не перевірений: якщо після правого повороту
курс у діагностиці ЗМЕНШУЄТЬСЯ — це треба повідомити.

## Межа

Без мережі й без OBD спуфер, що веде з правдоподібною швидкістю, поки ви
справді їдете, непомітний.

## Детектор руху

Акселерометр: розкид модуля прискорення > 0,35 м/с² — їдемо, < 0,15 три
секунди — стоїмо. На місці позиція усереднюється, швидкість нуль.

## Логи

Схема 7, 49 колонок, `Android/data/com.gnssfilter/files/logs/`, 14 діб.
Три джерела поруч (`gps_*`, `net_*`, `fus_*`), вихід (`out_*`), ознаки (`flags`),
рух (`acc_std`, `moving`, `motion_src`), OBD (`obd_speed`, `obd_link`), курс (`hdg`). Події: `TRUST`, `PROBE`, `PROBE_END`, `MOCK_*`, `AGC_BASE`.

## Груба мережа (v7.1)

За містом мережа дає фікс раз на 10-30 с із точністю 300-900 м, і сусідні
фікси стрибають у медіані на 717 м (лог 14.09). Віддавати їх напряму — це
телепортація. Тому в грубому режимі працює той самий принцип, що й у точному:
рух між фіксами рахуємо самі (швидкість з OBD або недавнього GPS, курс з
гіроскопа), а фікс лише підтягує оцінку з вагою за своєю точністю. Стрибок
понад 3 сигми йде в карантин до підтвердження другим фіксом; без жодного
злиття 30 с — приймаємо, ми переїхали. Радіус чесно включає незгоду з
останнім фіксом. На логах 14.09 стрибки понад 500 м — удвічі менше.

## ВАЖЛИВО: після кожного оновлення APK

Android скидає вибір mock-застосунку при перевстановленні. Лог 14.09: **5 год
40 хв** застосунок не міг ставити mock — навігатор бачив сирий підмінений GPS,
а фільтр лише спостерігав. Тепер це червоний заголовок «Мок заборонено» у
застосунку й «НЕ ПРАЦЮЄ» у сповіщенні, з вібрацією раз на дві хвилини.

## Налаштування

Developer options → Select mock location app → GNSS Filter — **після кожного оновлення**. Дозволи: точна
локація, сповіщення, показ поверх вікон. Поріг точності мережі — 150–200 м.

## Екран (v7.0)

Стан великим блоком кольору довіри; три ключові числа — радіус, швидкість,
супутники; джерело й OBD одним рядком; діагностика згорнута, розгортається
кнопкою. Налаштування зберігаються між запусками.

## NMEA — справжній GPS повз мок (v8.0)

`addNmeaListener` віддає речення з чіпа **напряму, повз шар провайдерів**. Тому
під тестовим провайдером `$GNGGA` / `$GNRMC` несуть СПРАВЖНЮ позицію приймача —
ту саму, заради якої раніше доводилось знімати mock на пробу.

Наслідки:
- Зона мережі, стрибок, фальшивий рух перевіряються **безперервно**, не
  показуючи навігатору нічого. Підміна ловиться під моком (подія `TRUST:
  підміна виявлена через NMEA`).
- Довіра повертається без проби — подія `NMEA_TRUST`.
- `GGA` дає кількість супутників у розв'язку з чіпа, що рятує там, де
  `GnssStatus` під моком мовчить (Note 20 / Android 13).
- Проба лишається запасним шляхом: тільки коли NMEA не приходить.

Розбираються лише GGA і RMC, з перевіркою контрольної суми; GSV та інше
відкидається за типом, до підрахунку суми. Точність оцінюється з HDOP
(≈ HDOP × 5 м, межі 3–100).

**Межа:** NMEA з'являється тільки коли приймач видає фікс. При повному
придушенні (тип 1) речень GGA немає взагалі — у лозі 10.09 за 6 хвилин лише
одне GGA з якістю 0. Тоді все як раніше: фізика й проба. Зате при **підміні**
(тип 2) фікс є завжди — і саме там NMEA найцінніший.
